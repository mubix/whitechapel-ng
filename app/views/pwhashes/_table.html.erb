<table class="table table-condensed">
  <thead>
    <th>Type</th>
    <th>Hash</th>
    <th>Source</th>
    <th>Cleartext</th>
  </thead>
  <tbody>
  <% @pwhashes.each do |pwhash| %>
    <tr>
      <td><%= pwhash.type.name %></td>
      <td><%= pwhash.password_hash %></td>
      <td><%= pwhash.status.name %></td>
      <td><%= link_to pwhash.password.cleartext, cleartext_lookup_path(pwhash.password.id)  %></td>
    </tr>
  <% end %>
  </tbody>
</table>
